/** @type {import('next').NextConfig} */
const nextConfig = {
  // Production-like dev UI: hide Next.js dev indicator (no floating "N" / build activity)
  devIndicators: {
    buildActivity: false,
  },
};

export default nextConfig;
